{#
  Contact

  @author Team phpManufaktur <team@phpmanufaktur.de>
  @link https://kit2.phpmanufaktur.de/Contact
  @copyright 2013 Ralf Hertsch <ralf.hertsch@phpmanufaktur.de>
  @license MIT License (MIT) http://www.opensource.org/licenses/MIT
#}

{{ alert }}

<div class="panel panel-default">
  <div class="panel-body">
    {% if app.translator.locale == 'de' %}
      {# GERMAN #}
      <p>Der <strong>Bezeichner</strong> für das Extrafeld wird immer in Großbuchstaben umgewandelt, Leerzeichen werden durch einen Unterstrich ersetzt, Sonderzeichen wie ä, ö, ü oder ß sind im Bezeichner nicht gestattet.</p>
      <p>In der Voreinstellung werden die <strong>Bezeichner</strong> in allen Dialogen so angezeigt, daß der erste Buchstabe in Großschreibung und alle folgenden Zeichen in Kleinschreibung dargestellt und Unterstriche durch Leerzeichen ersetzt werden.</p>
      <p>Sie können die <strong>Bezeichner</strong> in jede beliebige Sprache übersetzen, die Sie in Ihrem CMS nutzen. Fügen Sie den <strong>Bezeichner</strong> einfach einer benutzerdefinierten Sprachdatei im Verzeichnis <em>/kit2/extension/phpmanufaktur/phpManufaktur/Contact/Data/Locale/Custom</em> hinzu. Die <em>README.md</em> im gleichen Verzeichnis erklärt Ihnen, wie Sie benutzerdefinierte Sprachdateien anlegen und verwenden.</p>
      {% if form.extra_type_id.vars.value > 0 %}
        <p>In der aktuellen Sprache {{ app.translator.locale|upper }} wird der <strong>Bezeichner</strong> von <strong>{{ form.extra_type_name.vars.value|replace({'_':' '})|capitalize }}</strong> in <strong>{{ form.extra_type_name.vars.value|replace({'_':' '})|capitalize|trans }}</strong> übersetzt und angezeigt.</p>
      {% endif %}
    {% else %}
      {# ENGLISH #}
      <p>The <strong>name</strong> of the extra field will be always transformed to uppercase and spaces will be replaced by underscores, special chars are not allowed.</p>
      <p>By default the <strong>name</strong> will be shown in all dialogs with the first character in uppercase, all following characters in lowercase and underscores replaced with a space.</p>
      <p>You can <em>translate</em> the <strong>name</strong> to any language you are using within your CMS. Just add the <strong>name</strong> to a custom language file in the directory <em>/kit2/extension/phpmanufaktur/phpManufaktur/Contact/Data/Locale/Custom</em>, the <em>README.md</em> in this directory will explain you how to create and use custom language files.</p>
      {% if form.extra_type_id.vars.value > 0 %}
        <p>At the current language {{ app.translator.locale|upper }} the <strong>name</strong> will be translated from <strong>{{ form.extra_type_name.vars.value|replace({'_':' '})|capitalize }}</strong> to <strong>{{ form.extra_type_name.vars.value|replace({'_':' '})|capitalize|trans }}</strong>.</p>
      {% endif %}  
    {% endif %}
  </div>
</div>

{% form_theme form '@phpManufaktur/Contact/Template/default/pattern/form.fields.horizontal.twig' %}

<form action="{{ FRAMEWORK_URL ~ route.action }}" class="form-horizontal" method="post" role="form">
  
  <fieldset>
    <legend>{{ 'Extra field definition'|trans }}</legend>
    {{ form_widget(form) }}     
  </fieldset>
  
  <div class="form-group">
    <div class="col-sm-9 col-sm-offset-3">
      <button class="btn btn-default" type="submit">{{ 'Submit'|trans }}</button>
    </div>
  </div>
</form>    
